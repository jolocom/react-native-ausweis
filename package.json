{
  "name": "@jolocom/react-native-ausweis",
  "version": "2.0.3",
  "description": "React Native module for AusweisApp2",
  "main": "./js/index.js",
  "files": [
    "README.md",
    "android",
    "ios",
    "react-native-aa2-sdk.podspec",
    "js/*"
  ],
  "scripts": {
    "build": "tsc -p ./tsconfig.json",
    "clean": "rm -rf ./node_modules; rm -rf ./js",
    "prepare": "yarn build",
    "test": "jest --verbose",
    "prettier:fix": "prettier src/ --write",
    "prettier:check": "prettier src/ --check",
    "postinstall": "project_dir=$PWD; echo \"Notice: updating module \"; if [[ -f .gitmodules ]]; then sed -n -e '/path/,/url/p' .gitmodules | sed 'N;s/\\n/\\$$$/' | while IFS= read -r line; do if [[ $line =~ (.*)\\$\\$\\$(.*) ]] ; then path=\"$(echo ${BASH_REMATCH[1]} | sed 's/.*= //')\"; url=\"$(echo ${BASH_REMATCH[2]} | sed 's/.*= //')\"; if [[ -d \"${path}/.git\" ]] ; then echo \"Notice: git pull submodule $(realpath ${path})\"; cd \"${path}\" && git pull; else if [[ -z \"$(ls -A ${path})\" ]]; then echo \"Notice: git clone submodule $(realpath ${path})\"; rm -rf \"${path}\"; git clone $url $path; else echo \"Notice: $(realpath ${path}) submodule folder is not empty, try to remove it before clone.\"; fi; fi; else echo \"Notice: module does  not contain submodules.\"; fi; done; else echo \"Notice: module does not contain submodules.\"; fi; cd \"${project_dir}\";"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/jolocom/react-native-ausweis.git"
  },
  "keywords": [
    "Ausweis",
    "AusweisApp",
    "React Native AusweisApp",
    "eID",
    "Jolocom"
  ],
  "author": {
    "name": "Jolocom",
    "email": "dev@jolocom.com"
  },
  "license": "MIT",
  "licenseFilename": "LICENSE",
  "readmeFilename": "README.md",
  "peerDependencies": {
    "react": ">=16.8.1",
    "react-native": ">=0.60.0-rc.0 <1.0.x"
  },
  "devDependencies": {
    "@types/jest": "^29.4.0",
    "jest": "^29.4.3",
    "prettier": "^2.8.4",
    "react": "^16.9.0",
    "react-native": "^0.61.5",
    "ts-jest": "^29.0.5",
    "typed-emitter": "^2.1.0",
    "typescript": "^4.9.5"
  },
  "volta": {
    "node": "12.14.1",
    "yarn": "1.22.10"
  }
}
